# DiffDynamic — core + web dependencies
# Install into conda env `diffdynamic` (preferred) or: pip install -r requirements.txt

# Web / API
fastapi>=0.100.0
uvicorn>=0.20.0
sqlalchemy>=2.0
pydantic>=1.10
pyyaml>=5.4
python-multipart>=0.0.5

# Deep learning / chemistry (typically provided by conda env)
torch>=1.12.0
numpy>=1.21
pandas>=1.3
rdkit-pypi>=2022.3.0; platform_system!="Linux" or python_version>="3.9"
scipy>=1.7
tqdm>=4.62
openpyxl>=3.0
biopython>=1.79

# Optional docking helpers (system AutoDock Vina still required)
meeko>=0.5.0
